<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Superhéroes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" defer></script>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1>Superhéroes</h1>
    <ul>
        <% superheroes.forEach(superheroe => { %>
            <li><%= superheroe.nombreSuperheroe %> - <%= superheroe.nombreReal %></li>
        <% }) %>
    </ul>

    

<main class="flex-grow-1 p-4">

    <!-- Seccion de Listar -->
    <div>
        <h2 class="fs-3 mb-3">Listado de SuperHeroes</h2>
        <p>Aquí va el listado de superheroes</p>
        <div>
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Superheroe</th>
                            <th>Nombre Real</th>
                            <th>Edad</th>
                            <th>Planeta</th>
                            <th>Enemigos</th>
                            <th>Poder</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% superheroes.forEach(element => { %>
                            <tr>
                                <td><%= element.nombreSuperheroe %></td>
                                <td><%= element.nombreReal %></td>
                                <td><%= element.edad %></td>
                                <td><%= element.planetaOrigen %></td>
                                <td><%= element.enemigos %></td>
                                <td><%= element.poderes.join(', ') %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</main>

</body>
</html>
